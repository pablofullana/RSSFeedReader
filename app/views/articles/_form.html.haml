
- if @article.errors.any?
  #error_explanation
    %h2= "#{pluralize(@article.errors.count, "error")} prohibited this article from being saved:"
    %ul
      - @article.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :title
  = f.text_field :title
.field
  = f.label :link
  = f.text_field :link
.field
  = f.label :description
  = f.text_area :description
.field
  = f.label :pub_date
  = f.date_select :pub_date
.field
  = f.label :comments
  = f.text_area :comments
.field
  = f.label :starred
  = f.check_box :starred
.field
  = f.label :channel_id
  = f.number_field :channel_id
.actions
  = f.submit 'Save'
